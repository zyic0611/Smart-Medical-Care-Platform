worker_processes 1;
events { worker_connections 1024; }

http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile        on;

    server {
        listen       80;
        server_name  localhost;
        client_max_body_size 100M;

        # 🌟 定义允许跨域的源 (根据请求头动态匹配，比写死更灵活)
        set $cors_origin "";
        if ($http_origin ~* "^http://localhost:5173$") {
            set $cors_origin $http_origin;
        }

        # =========================================================
        # 规则1：转发给 Python AI 服务 (这里是报错的高发区)
        # =========================================================
        location ^~ /api/diagnosis/result {
            # 1. 预检请求 (OPTIONS) - Nginx 直接拦截处理，不发给 Python
            if ($request_method = 'OPTIONS') {
                add_header 'Access-Control-Allow-Origin' $cors_origin always;
                add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, DELETE' always;
                add_header 'Access-Control-Allow-Headers' 'token,Authorization,Content-Type,Accept,Origin,X-Requested-With' always;
                add_header 'Access-Control-Allow-Credentials' 'true' always;
                add_header 'Content-Length' 0;
                add_header 'Content-Type' 'text/plain; charset=utf-8';
                return 204;
            }

            # 2. 转发给 Python 容器 (使用 Docker 服务名)
            proxy_pass http://medical_diagnosis_api:9091;

            # 🌟🌟🌟 关键修复点 🌟🌟🌟
            # 强制隐藏/丢弃 Python 代码里返回的跨域头
            # 这样浏览器就只会看到 Nginx 发出的头，不会报 "multiple values" 错误
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Credentials;
            proxy_hide_header Access-Control-Allow-Methods;

            # 3. 添加 Nginx 自己的跨域头
            add_header 'Access-Control-Allow-Origin' $cors_origin always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;

            # 4. 标准代理头设置
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        # =========================================================
        # 规则2：转发给 Java 后端 (本地 8080)
        # =========================================================
        location / {
            # 1. 预检请求
            if ($request_method = 'OPTIONS') {
                add_header 'Access-Control-Allow-Origin' $cors_origin always;
                add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, DELETE' always;
                add_header 'Access-Control-Allow-Headers' 'token,Authorization,Content-Type,Accept,Origin,X-Requested-With' always;
                add_header 'Access-Control-Allow-Credentials' 'true' always;
                return 204;
            }

            # 2. 转发给宿主机 Java
            proxy_pass http://host.docker.internal:8080;

            # 同样建议隐藏上游头，防止 Java 那边以后加了跨域导致冲突
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Credentials;

            # 3. 添加跨域头
            add_header 'Access-Control-Allow-Origin' $cors_origin always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}